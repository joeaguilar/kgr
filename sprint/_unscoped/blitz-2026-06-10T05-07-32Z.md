# Blitz wave - 2026-06-10T05-07-32Z

## Config

- Tracker: `itr ready -f json --fields id,title,urgency,status,priority,kind,files,blocked_by,parent_id`
- Close command: `itr close <ID> "<reason>"`
- Dep graph: `kgr check --format json --no-progress .` present; non-blocking known cycles reported in `crates/kgr-core/src/parse/*` and `tests/fixtures/typescript/cycle/*`
- Verify gate: `just verify`
- Concurrency: 5
- Repo: `/Users/goldboy/AI_Projects/kgr`
- Stop when: current wave completes; run full gate before planning another wave

## Waves

### Wave 1

| Issue | Title | Owned files |
|---|---|---|
| #11 | Fix rule glob anchoring so common directory patterns are not silently dead | `crates/kgr/src/rules.rs` |
| #12 | Strengthen parse-cache key to avoid same-second/same-size stale reads; add cache bypass | `crates/kgr/src/cache.rs` |
| #33 | Rust bare-import resolution can draw phantom edges to coincidentally-named local modules | `crates/kgr-core/src/resolve.rs` |
| #37 | Make dead distinguish self-only references from live cross-file use | `crates/kgr/src/main.rs`, `crates/kgr/tests/symbols.rs` |
| #2 | Port itr's auto-version workflow: automate semver tagging + release dispatch | `.github/workflows/auto-version.yml`, `.github/workflows/release.yml`, `build.rs` |

## File Conflicts

- No intra-wave file conflicts.
- Resolver follow-up issues #16, #17, #18, #19, #23, and #35 all touch `crates/kgr-core/src/resolve.rs`; keep them out of this wave while #33 owns that file.
- Release issue #4 also touches `.github/workflows/release.yml`; keep it out of this wave while #2 owns that file.

## Semantic Warnings

- #33 changes Rust resolver semantics. Do not combine with Python/root/tsconfig resolver changes until the `resolve.rs` gate is green.
- #37 changes the `dead` command contract. Avoid changing shared symbol-query contracts from other tasks in the same wave.
- #12 changes cache key semantics and may interact with CLI cache-bypass behavior. Keep changes confined to cache behavior unless tests require a narrow CLI hook.

## Interventions

- 2026-06-10T05:07:32Z: Orchestrator preflighted `just verify` successfully before launch.
- 2026-06-10T15:57:02Z: Marked #11, #12, #33, #37, and #2 in progress and launched five worker agents: James (#11), Peirce (#12), Ampere (#33), Arendt (#37), Aristotle (#2).
- 2026-06-10T16:xx:xxZ: Arendt completed #37 edits but left the task open because `just verify` was red on #11's owned `crates/kgr/src/rules.rs` (`RuleViolation` missing `Debug`). Will close #37 after the wave gate is green.
- 2026-06-10T16:xx:xxZ: Aristotle completed #2 edits and `actionlint` passed, but left the task open because `just verify` was red on #33's owned `crates/kgr-core/src/resolve.rs`. Will close #2 after the wave gate is green.
- 2026-06-10T16:xx:xxZ: James completed #11 edits; `cargo test -p kgr rules` passed and `just verify` reached fmt-check, then failed on #33's owned `crates/kgr-core/src/resolve.rs`. Will close #11 after the wave gate is green.

## Outcomes

- #11: closed - rule globs now use literal separators, anchoring semantics are documented, likely-dead rule diagnostics are emitted, and targeted rules tests pass; orchestrator closed after wave gate passed.
- #12: closed - cache entries now compare subsecond mtime nanoseconds alongside size; same-second/same-size regression covered; `just verify` green in worker.
- #33: closed - Rust bare imports now resolve only through parsed module declarations, not coincidental external-crate-name shadow files; `just verify` green in worker.
- #37: closed - `dead` now separates self-file and cross-file references; orchestrator closed after wave gate passed.
- #2: closed - auto-version workflow added, release checkout fetches tag history, and `build.rs` watches `.git/packed-refs`; orchestrator closed after wave gate passed.

## Wave Gate

- 2026-06-10T16:06:xxZ: `just verify` passed from the orchestrator after all workers reached terminal state.
