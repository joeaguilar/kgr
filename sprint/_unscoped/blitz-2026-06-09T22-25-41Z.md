# Blitz — 2026-06-09 review-batch clearance

## Config

- Tracker: itr (`itr ready -f json` / `ITR_AGENT=claude itr close <ID> "reason"`)
- Verify gate: `just verify` (cargo check + clippy -D warnings + tests + fmt check) — baseline green at c643d14
- Concurrency: 5 | Max waves: 5 | Repo: /Users/goldboy/AI_Projects/kgr
- Scope: 25 tasks from the 2026-06-09 code-review batch (#46–#114) — all 16 highs + 9 file-disjoint mediums
- kgr check at start: ok=false, 2 cycles (known, tracked #32), 0 rule violations

## Waves

### Wave 1
| Task | Title | Owns |
|---|---|---|
| #49 | Single-file PATH silently yields empty results | walk.rs, pipeline.rs, main.rs |
| #50 | JS/TS resolver: with_extension truncates dotted specifiers | resolve.rs |
| #51 | python: decorated defs produce no symbols | parse/python.rs |
| #52 | ts/js: const arrow/function-expr/generator defs no symbols | parse/typescript.rs, parse/javascript.rs |
| #57 | cpp: inline class-body methods missing from symbols | parse/cpp.rs |

### Wave 2
| Task | Title | Owns |
|---|---|---|
| #48 | rust: scoped calls invisible to refs/dead/impact | parse/rust_lang.rs, main.rs |
| #54 | Resolve Ruby/PHP/Lua/Bash local imports | resolve.rs |
| #53 | php: require_once/parens/double-quoted/grouped-use missed | parse/php.rs |
| #58 | c/cpp: body-less specifiers create phantom Class symbols | parse/c.rs, parse/cpp.rs |
| #59 | elixir: guarded/paren-less defs + defmacro missing | parse/elixir.rs |

### Wave 3
| Task | Title | Owns |
|---|---|---|
| #47 | kgr init silently overwrites .kgr.toml | config.rs, main.rs |
| #55 | zig: sibling @import External, never resolved | parse/zig.rs, resolve.rs |
| #60 | Make fixture tests cache-hermetic | cache.rs, pipeline.rs, tests/snapshots.rs, tests/integration.rs |
| #89 | csharp: records missing; generic calls invisible | parse/csharp.rs |
| #90 | java: records missing; interface members non-exported | parse/java.rs |

### Wave 4
| Task | Title | Owns |
|---|---|---|
| #46 | Wire up dead .kgr.toml fields | config.rs, main.rs |
| #56 | go: module-path imports External, no go.mod handling | parse/go.rs, resolve.rs |
| #61 | Add tests for kgr query modes + untested flags | tests/integration.rs (main.rs READ-ONLY) |
| #85 | ruby: def self.x / class Foo::Bar no symbols | parse/ruby.rs |
| #93 | scala: grouped imports captured as verbatim blob | parse/scala.rs |

### Wave 5
| Task | Title | Owns |
|---|---|---|
| #77 | rust: mod resolves sibling before child dir | resolve.rs |
| #72 | DOT render invalid on quotes/backslashes | render/dot.rs |
| #75 | Table LOCAL-OUT counts deduped parallel imports | render/table.rs |
| #76 | Tree renders nothing for fully-cyclic graphs | render/tree.rs |
| #87 | lua: M.foo / M:bar / local fn no symbols | parse/lua.rs |

## File conflicts (serialized across waves)

- `main.rs`: #49→W1, #48→W2, #47→W3, #46→W4 (one per wave; #61 W4 treats it read-only)
- `resolve.rs`: #50→W1, #54→W2, #55→W3, #56→W4, #77→W5
- `parse/cpp.rs`: #57→W1, #58→W2
- `pipeline.rs`: #49→W1, #60→W3
- `tests/integration.rs`: #60→W3, #61→W4
- Deferred to a future run due to conflicts with in-run tasks: #81/#82 (typescript.rs), #83/#84 (php.rs), #86/#111 (ruby.rs), #91/#92 (cpp/c refs+typedef), #94 (elixir.rs), #98 (zig.rs), #97/#114 (rust_lang.rs), #109/#78/#79 (resolve.rs), #71/#73 (mermaid.rs), and all main.rs-only mediums (#62–#66, #70, #74, #105–#107)

## Semantic warnings

- GLOBAL: never run `just update-snapshots`, `cargo insta accept`, or `cargo insta test --accept` — they accept ALL pending snapshots including neighbors' in-flight breakage. Hand-edit only the .snap files your change provably affects.
- #60 (W3): do NOT add a --no-cache CLI flag this run — main.rs is owned by #47's agent in the same wave. Use tempdir-copied fixtures in tests and/or a build fingerprint in CACHE_VERSION (cache.rs).
- #61 (W4): main.rs is owned by #46's agent in the same wave — tests only, no src edits. Query bugs #62–#66 are open: assert correct current behavior, do not bake in buggy edge semantics (e.g. don't assert empty-JSON stdout shapes).
- #54 (W2): php.rs is being edited by #53 in the same wave — write resolver unit tests against synthetic known_files lists, not the php parser.
- #75/#76 (W5): touch only your own format's .snap files (table_* vs tree_*).
- #46 (W4): #20 (--no-external no-op) is open and out of scope — wiring config no_external into renderers is NOT required; focus on languages/format/no_progress/depth/entry/no_color per acceptance.

## Interventions

1. **W1 (all agents):** background agents cannot receive Bash permission prompts — `just verify`, `cargo test`, and `itr close` were denied for all five agents. All five completed their edits and stopped per protocol. Orchestrator ran the gate and closed the tasks (inspect-and-close fallback).
2. **W1 #49:** clippy `-D warnings` red — 11 × `ptr_arg` (`&PathBuf` → `&Path`) in the new/changed `run_*` signatures in main.rs. Orchestrator fixed all 11.
3. **W1 #50/#52:** rustfmt drift in resolve.rs and typescript.rs test code. Orchestrator ran `cargo fmt --all` after all agents were terminal (safe — no in-flight edits), then re-ran the gate.

## Outcomes

### Wave 5 — complete, gate green (exit 0), zero interventions
| Task | Outcome |
|---|---|
| #77 | closed (self) — module_dir candidate now beats from_dir sibling for mod declarations, 4 tests |
| #72 | closed (self) — DOT escaping for quotes/backslashes at all interpolation sites, 5 tests |
| #75 | closed (self) — table LOCAL-OUT uses kgraph.out_degree, balance tests, snapshots unchanged |
| #76 | closed (self) — zero-root tree renders Cycles: section, unit + integration tests |
| #87 | closed (self) — lua dotted/method/assigned function symbols, 5 tests |

All 25 originally-planned tasks closed across 5 waves. Final gate exit 0.

### Wave 6 — complete, gate green (exit 0)
| Task | Outcome |
|---|---|
| #116 | closed (orchestrator) — ts_language() in all 19 parsers, TSX-aware parse_errors, registry pin test; check --syntax now real; blocked only by mermaid fmt drift at agent time |
| #66 | closed (self) — validate_format_or_exit on all 11 subcommands incl. config/env-sourced formats, 3 integration tests |
| #78 | closed (self) — TS paths semantics: exact keys, longest-prefix, all targets, baseUrl anchoring, 6 tests |
| #71/#73 | closed (self) — deterministic n0/n1 mermaid IDs + entity escaping + BTreeSet cycle styles; 4 snaps hand-rewritten + new ts/cycle snap |
| #102 | closed (self) — 7 vacuous tests hardened in symbols.rs |
W6 intervention: (10) orchestrator tightened check_syntax_flag_reports_parse_errors to full assertions after #116 landed; test passes against real output.

### Wave 7 — complete, gate green (exit 0): 9 issues in 5 slots
#81/#82 (ts require-imports + abstract classes), #83/#84 (php static/new refs + trait/enum symbols), #86/#111 (ruby constant receivers + load/autoload/interpolation), #91/#92 (cpp template refs + c typedef structs), #94 (elixir pseudo-call exclusion). All work agent-authored; tasks closed by orchestrator after gate.
W7 interventions: (11) hand-applied rustfmt's expected form to ruby.rs — its agent died waiting and the drift would never self-clear, wedging every live agent's gate; (12) same for one line in typescript.rs after its agent went quiet 10+ min; orchestrator then ran the gate (exit 0) and closed all 9.

### Wave 8 — complete, gate green (exit 0): 7 issues in 5 slots
#62/#105 (dead/impact: found:false contract, definitions arrays, union blast radius, agent_docs updated — self-closed), #79 (angle includes keep System kind, include/-root search only — self-closed), #95 (haskell binds + export list — orchestrator-closed, stranded on neighbor fmt), #96/#113 (objc @import + one-symbol-per-method — self-closed), #97 (rust type/union/macro_rules/trait-sig symbols — orchestrator-closed, stranded). The own-fmt-before-final-gate prompt rule eliminated wedges this wave; stranded agents were wait-loop deaths only.

### Wave 9 — complete, gate green (exit 0): 7 issues in 5 slots
#64/#65/#107 (query JSON empty results, target validation, selector mutual exclusion — self-closed), #80 (`.mts`/`.cts`/`.mm` detection plus TS resolver candidates — self-closed), #88 (bash source first-argument-only imports — self-closed), #98 (zig top-level symbol filtering, discard skip, container consts as Class — orchestrator-closed after neighbor test landed), #112 (python literal `__import__`/`importlib.import_module` dynamic imports — self-closed). Final gate: `just verify` exit 0 (44 + 65 + 21 + 35 + 444 tests, fmt check).
W9 intervention: (13) #98's agent completed its owned Zig fix but left the issue open because the gate was red in neighbor-owned `tests/integration.rs`; after slot #64/#65/#107 landed and self-verified, orchestrator re-ran `just verify` (green) and closed #98.

### Wave 10 — complete, gate green (exit 0): 7 issues in 5 slots
| Task | Outcome |
|---|---|
| #63/#74/#106 | closed (orchestrator) — direct `--who-imports`, fixed agent-docs synopsis, `query --heaviest --top`, 4 integration/doc tests |
| #109 | closed (self) — Go relative package resolution now ignores nested subpackages, 1 resolver regression |
| #67 | closed (orchestrator) — invalid exclude globs emit named warnings while valid excludes still apply, 2 walk unit tests |
| #104 | closed (orchestrator) — parse cache prunes entries missing from current walk, cache + pipeline regressions |
| #101 | closed (orchestrator) — JSON graph snapshots added for python/simple, ts/simple, js/simple, js/mixed, rust/local_modules |
W10 interventions: (14) #67's agent stopped responding after leaving a complete `walk.rs` implementation; orchestrator shut it down, inspected the single-file diff, and carried it through the wave gate. (15) After all agents were terminal, orchestrator ran `cargo fmt --all` once to resolve formatter drift in `walk.rs`/`integration.rs`; `just verify` then passed (48 + 67 + 26 + 35 + 445 tests, fmt check).

### Extension (user-approved): continue waves over remaining review-batch backlog
Same model; same-file sibling issues bundled into one agent slot. Wave 6: #116 (--syntax no-op, owns parse/*), #66 (main.rs + integration.rs), #78 (resolve.rs), #71+#73 (mermaid.rs + snapshots.rs), #102 (tests/symbols.rs). Orchestrator tightens the softened check_syntax integration test after #116 lands (avoids integration.rs ownership conflict with #66).

### Wave 4 — complete, gate green (exit 0)
| Task | Outcome |
|---|---|
| #46 | closed (orchestrator) — config languages/format/no_progress wired as CLI defaults, dead fields removed, KGR_FORMAT works, 14 tests |
| #56 | closed (orchestrator) — go.mod module-path resolution (direct-child files only), raw_string_literal capture |
| #61 | closed (orchestrator) — query modes + flag test coverage; 5 tests orchestrator-aligned (see interventions) |
| #85 | closed (orchestrator) — ruby singleton_method + scoped class/module symbols, 4 tests |
| #93 | closed (orchestrator) — scala grouped/comma/rename import expansion, 7+ tests |

W4 interventions: (6) #61's agent wrote 5 tests asserting behavior blocked by open bugs — 4 relied on python `from . import X` edges (#16); orchestrator re-pointed them at TS fixtures/tempdirs with real resolved edges. (7) The 5th test exposed a NEW bug: `check --syntax` is a complete no-op (no parser overrides ts_language(), parse_errors always empty) — filed as #116, test softened to pin the JSON shape with a tighten-later comment. (8) All four code agents finished cleanly but couldn't close on #61's red; orchestrator verified at the wave gate and closed all five. (9) cargo fmt --all post-wave for drift in orchestrator's own test edits.
W4 pattern note: agents that yield mid-wait leave tasks stranded — "sleep watchers" die with the agent. Inspect-and-close handled all of them; no work was lost.

### Wave 3 — complete, gate green (exit 0)
| Task | Outcome |
|---|---|
| #47 | closed (orchestrator) — init overwrite guard + --force; agent's gate was blocked only by #55's transient red, work verified at wave gate |
| #55 | closed (self) — .zig/`/` Local classification + resolve_zig arm, 10 tests |
| #60 | closed (self) — CACHE_VERSION build fingerprint, KGR_NO_CACHE bypass, hermetic test helpers, 12 tests, fixture cache litter deleted |
| #89 | closed (self) — C# record symbols + generic/qualified call refs, 10 tests |
| #90 | closed (self) — Java record symbols + interface implicit-public exported, 4 tests |

W3 interventions: (4) closed #47 at the wave gate (inspect-and-close; its watcher died with the agent). (5) Applied #60's residual recommendation — added .env("KGR_NO_CACHE","1") to all 35 cargo_bin_cmd! sites in tests/symbols.rs to stop fixture cache litter; gate re-run green, 0 cache files remain under tests/fixtures/.

### Wave 2 — complete, gate green (exit 0), zero interventions
| Task | Outcome |
|---|---|
| #48 | closed (self) — scoped_identifier call/macro patterns + callee_matches(::) helper in main.rs, 7 tests |
| #54 | closed (self) — resolver arms for Ruby/PHP/Lua/Bash, 19 unit tests |
| #53 | closed (self) — require_once/include_once/parens/encapsed/grouped-use capture, 13 tests |
| #58 | closed (self) — body: constraints kill phantom C/C++ Class symbols, 5 regression tests |
| #59 | closed (self) — guarded/paren-less defs + defmacro symbols, 7 tests |

Allowlist change before W2 (user-approved option B): added just verify / cargo check|test|clippy / cargo fmt --check / itr to .claude/settings.local.json; validated by a smoke-test agent (all allowed, gate green). Agents self-verified and self-closed; observed correct self-healing behavior (waiting out neighbor-owned transient reds instead of cross-editing).

### Wave 1 — complete, gate green (exit 0)
| Task | Outcome |
|---|---|
| #49 | closed — single-file PATH support (walk/main/pipeline), 7 integration tests |
| #50 | closed — extension append + js-family-only substitution in resolver, 7 unit tests |
| #51 | closed — decorated_definition patterns in python symbols, 4 unit tests |
| #52 | closed — const arrow/function-expr/generator symbols in ts/js/tsx, 13 unit tests |
| #57 | closed — inline class-body Method extraction in cpp, 2 unit tests |
